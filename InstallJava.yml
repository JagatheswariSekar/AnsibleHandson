- name: Install Java
  hosts: testservers
  tasks:
    - name: Update APT package manager repositories cache
      become: true
      yum:
        update_cache: yes
    - name: Install Java using Ansible

      yum:
        name: "{{ packages }}"
      vars:
        packages:
           - java-1.8.0-openjdk
